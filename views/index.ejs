<!DOCTYPE html>
<html lang="en">

<head>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="">

  <title>Pic of the Day</title>

<style>
  
/*CONTAINERS*/
body {background: #0d1111; margin:0;}
main {
  width: 90%;
  max-width: 800px;
  margin: 0 auto;
  padding: 16px 8px;
}

/*TEXT*/
h1 {
  color: #111;
  font-size: 36px;
  font-weight: 100;
  margin: 0;
}
p {
    color: #777;
    font-family: sans-serif;
    font-size: 12px;
    font-weight: 100;
    line-height: 1.5;
}

/*ARTICLE*/
.article {
  background-color: #fafafa;
  border-radius: 8px;
  padding: 24px 16px 40px;

  img {
    background: rgba(172,199,204,0.2);
    border: 8px solid rgba(172,199,204,0.2);;
    border-radius: 16px;
    max-width: 632px;
    width: calc(100% - 64px);
    margin: 16px auto 32px;
    padding: 8px;
    display: block;
  }
}

.text-intro {
  max-width: 580px;
  margin: 40px auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  h1 {
    width: 40%;
  }
  
  p {
    width: 60%;
    margin: 0 0 0 16px;
  }
}
.text-body {
  max-width: 580px;
  margin: 0 auto;
  
  p {
    font-size: 12px;
  }
}
</style>
</head>

<body>
  <main>
  
    <section class="article">
      <% if(gallery.media_type=='image'){%>
      <div style="width:100%;height:500px;">
      <img src="<%= gallery.hdurl %>" style="width:100%;height:100%;" alt="home layout">
    </div>
    <%}else{%>
      <iframe width="100%" height="500px" src="<%= gallery.url %>">
      </iframe>
      <%}%>
      <div class="text-intro">
      <h1><%= gallery.title %></h1>
       
      </div>
      <div class="text-body">
      <h4>Posted on <%= moment(gallery.date).format('MMM DD, YYYY') %></h4>
      <p><%= gallery.explanation %></p>
      </div>
    </section>
    
  </main>
  <!-- /.container -->

  <!-- Footer -->
 
    <div class="container" style="text-align: center;color:#fafafa;padding: auto;">
      <strong>Copyright &copy; Divyanshu Shukla</strong>
    </div>
  
</body>

</html>
